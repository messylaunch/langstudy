# Optional build-time configuration. You can also paste these values into the
# app's Settings screen at runtime instead (stored in the browser).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
